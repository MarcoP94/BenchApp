import { StyleSheet, Dimensions } from "react-native";
const styles = StyleSheet.create({
  flatContainer: {
    flex: 1,
  },
  opContainer: {
    marginLeft: "auto",
    marginRight: "auto",
  },
  title: {
    fontWeight: "600",
    fontSize: 20,
  },
});

export default styles;
